"""
  Phase 5: Agent 实现

    baseagent: 应负责所有agent的共有能力

    plannerAgent: 负责生成AnalysisPlan （任务列表，关注问题，需要阅读的章节，是否retrieval）

    readerAgent: 读论文的Agent（论文研究问题、背景、主要贡献。。。不批判论文，只做阅读者。

    criticAgent: 批判可以尖锐，但必须基于ReaderNotes和EvidenceBundle，不能凭空审稿

    writerAgent: 把前面所欲结构化成果整合成最终报告

"""